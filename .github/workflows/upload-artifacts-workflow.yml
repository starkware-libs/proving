name: upload artifacts

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  # Publishes the supported-circuits trust anchors: per circuit family, the `circuit_params
  # --registry` JSON, at supported_circuits_registry/<commit>/<family>/registry.json.
  supported-circuits-registry-push:
    runs-on: namespace-profile-starkware-libs-proving-dev-medium-ubuntu-24-04-amd64
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Get commit hash prefix for PR update
        env:
          COMMIT_SHA: ${{ github.sha }}
        run: |
          echo "SHORT_HASH=${COMMIT_SHA:0:7}" >> $GITHUB_ENV

      - name: Authenticate with GCS
        uses: google-github-actions/auth@c200f3691d83b41bf9bbd8638997a462592937ed # v2
        with:
          credentials_json: ${{ secrets.SHARP_BINARY_ARTIFACTS_WRITER_SA }}

      - name: Build circuit-params
        run: cargo build --release -p circuit-params

      # The circuit families (params/program files and trace ranges) are defined in the script.
      - name: Generate the supported circuits registries
        run: |
          python3 .github/scripts/generate_circuit_registries.py \
            --circuit-params-binary target/release/circuit-params \
            --output-dir registry_out

      - name: Upload supported circuits registry to GCP
        uses: google-github-actions/upload-cloud-storage@c0f6160ff80057923ff50e5e567695cea181ec23 # v2
        with:
          path: "registry_out"
          destination: "sharp-binary-artifacts/supported_circuits_registry/${{ env.SHORT_HASH }}"
          parent: false

  artifacts-push:
    runs-on: namespace-profile-starkware-libs-proving-dev-medium-ubuntu-24-04-amd64
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Get commit hash prefix for PR update
        env:
          COMMIT_SHA: ${{ github.sha }}
        run: |
          echo "SHORT_HASH=${COMMIT_SHA:0:7}" >> $GITHUB_ENV

      - name: Authenticate with GCS
        uses: google-github-actions/auth@c200f3691d83b41bf9bbd8638997a462592937ed # v2
        with:
          credentials_json: ${{ secrets.SHARP_BINARY_ARTIFACTS_WRITER_SA }}

      - name: Setup scarb
        uses: software-mansion/setup-scarb@2a96b748888e3329ee44ac9ac073d930e692b3cd # v1
        with:
          tool-versions: .tool-versions

      - name: Build and upload stwo_cairo_verifier
        uses: ./.github/actions/build-verifier
        with:
          package: stwo_cairo_verifier
          features: qm31_opcode,poseidon_outputs_packing
          out: stwo_cairo_verifier.json

      - name: Build and upload stwo_cairo_poseidon_verifier
        uses: ./.github/actions/build-verifier
        with:
          package: stwo_cairo_verifier
          features: poseidon252_verifier,poseidon_outputs_packing
          out: stwo_cairo_poseidon_verifier.json

      - name: Build and upload stwo_cairo_verifier_with_blake_packing
        uses: ./.github/actions/build-verifier
        with:
          package: stwo_cairo_verifier
          features: qm31_opcode,blake_outputs_packing
          out: stwo_cairo_verifier_with_blake_packing.json

      - name: Build and upload mock_verifier
        uses: ./.github/actions/build-verifier
        with:
          package: stwo_cairo_verifier_mock
          features: qm31_opcode
          out: mock_verifier.json

      - name: Build and upload stwo_circuit_verifier
        uses: ./.github/actions/build-verifier
        with:
          package: stwo_circuit_verifier
          features: qm31_opcode
          out: stwo_circuit_verifier.json

      - name: Upload casm_registry.json to GCP
        id: upload_casm_registry
        uses: google-github-actions/upload-cloud-storage@c0f6160ff80057923ff50e5e567695cea181ec23 # v2
        with:
          path: "crates/common/casm_registry.json"
          destination: "sharp-binary-artifacts/casm_registry_artifacts/${{ env.SHORT_HASH }}/release"

      - name: Build all generic binaries
        run: |
          cargo build --release

      - name: Upload cairo_program_runner x86_64 binary to GCP
        id: upload_cairo_program_runner_x86
        uses: google-github-actions/upload-cloud-storage@c0f6160ff80057923ff50e5e567695cea181ec23 # v2
        with:
          path: "target/release/cairo_program_runner"
          destination: "sharp-binary-artifacts/cairo_program_runner_artifacts/${{ env.SHORT_HASH }}/release"

      - name: Rename stwo-vm-runner binary
        run: mv target/release/stwo-vm-runner target/release/stwo_vm_runner

      - name: Upload stwo_vm_runner x86_64 binary to GCP
        id: upload_stwo_vm_runner_x86
        uses: google-github-actions/upload-cloud-storage@c0f6160ff80057923ff50e5e567695cea181ec23 # v2
        with:
          path: "target/release/stwo_vm_runner"
          destination: "sharp-binary-artifacts/stwo_vm_runner_artifacts/${{ env.SHORT_HASH }}/release"

      - name: Rename stwo-run-and-prove binary
        run: mv target/release/stwo-run-and-prove target/release/stwo_run_and_prove

      - name: Upload stwo_run_and_prove binary to GCP
        id: upload_stwo_run_and_prove
        uses: google-github-actions/upload-cloud-storage@c0f6160ff80057923ff50e5e567695cea181ec23 # v2
        with:
          path: "target/release/stwo_run_and_prove"
          destination: "sharp-binary-artifacts/stwo_run_and_prove_artifacts/${{ env.SHORT_HASH }}/release"

      - name: Rename leaf-prover binary
        run: mv target/release/leaf-prover target/release/leaf_prover

      - name: Upload leaf_prover x86_64 binary to GCP
        id: upload_leaf_prover
        uses: google-github-actions/upload-cloud-storage@c0f6160ff80057923ff50e5e567695cea181ec23 # v2
        with:
          path: "target/release/leaf_prover"
          destination: "sharp-binary-artifacts/leaf_prover_artifacts/${{ env.SHORT_HASH }}/release"

      - name: Upload stwo_run_and_prove_recursive_tree binary to GCP
        id: upload_stwo_run_and_prove_recursive_tree
        uses: google-github-actions/upload-cloud-storage@c0f6160ff80057923ff50e5e567695cea181ec23 # v2
        with:
          path: "target/release/stwo_run_and_prove_recursive_tree"
          destination: "sharp-binary-artifacts/stwo_run_and_prove_recursive_tree/${{ env.SHORT_HASH }}/release"

      - name: Build and upload stwo_run_and_prove for znver5
        uses: ./.github/actions/build-arch-binary
        with:
          arch: znver5
          binary_name: stwo-run-and-prove

      - name: Build and upload stwo_run_and_prove for znver4
        uses: ./.github/actions/build-arch-binary
        with:
          arch: znver4
          binary_name: stwo-run-and-prove

      - name: Build and upload stwo_run_and_prove for znver3
        uses: ./.github/actions/build-arch-binary
        with:
          arch: znver3
          binary_name: stwo-run-and-prove

      - name: Build and upload stwo_run_and_prove for emeraldrapids
        uses: ./.github/actions/build-arch-binary
        with:
          arch: emeraldrapids
          binary_name: stwo-run-and-prove

      - name: Build and upload stwo_run_and_prove for sapphirerapids
        uses: ./.github/actions/build-arch-binary
        with:
          arch: sapphirerapids
          binary_name: stwo-run-and-prove

      - name: Build and upload stwo_run_and_prove for graniterapids
        uses: ./.github/actions/build-arch-binary
        with:
          arch: graniterapids
          binary_name: stwo-run-and-prove

      - name: Build and upload leaf_prover for znver5
        uses: ./.github/actions/build-arch-binary
        with:
          arch: znver5
          binary_name: leaf-prover

      - name: Build and upload leaf_prover for znver4
        uses: ./.github/actions/build-arch-binary
        with:
          arch: znver4
          binary_name: leaf-prover

      - name: Build and upload leaf_prover for znver3
        uses: ./.github/actions/build-arch-binary
        with:
          arch: znver3
          binary_name: leaf-prover

      - name: Build and upload leaf_prover for emeraldrapids
        uses: ./.github/actions/build-arch-binary
        with:
          arch: emeraldrapids
          binary_name: leaf-prover

      - name: Build and upload leaf_prover for sapphirerapids
        uses: ./.github/actions/build-arch-binary
        with:
          arch: sapphirerapids
          binary_name: leaf-prover

      - name: Build and upload leaf_prover for graniterapids
        uses: ./.github/actions/build-arch-binary
        with:
          arch: graniterapids
          binary_name: leaf-prover

      - name: Build and upload stwo_run_and_prove_recursive_tree for znver5
        uses: ./.github/actions/build-arch-binary
        with:
          arch: znver5
          binary_name: stwo_run_and_prove_recursive_tree

      - name: Build and upload stwo_run_and_prove_recursive_tree for znver4
        uses: ./.github/actions/build-arch-binary
        with:
          arch: znver4
          binary_name: stwo_run_and_prove_recursive_tree

      - name: Build and upload stwo_run_and_prove_recursive_tree for znver3
        uses: ./.github/actions/build-arch-binary
        with:
          arch: znver3
          binary_name: stwo_run_and_prove_recursive_tree

      - name: Build and upload stwo_run_and_prove_recursive_tree for emeraldrapids
        uses: ./.github/actions/build-arch-binary
        with:
          arch: emeraldrapids
          binary_name: stwo_run_and_prove_recursive_tree

      - name: Build and upload stwo_run_and_prove_recursive_tree for sapphirerapids
        uses: ./.github/actions/build-arch-binary
        with:
          arch: sapphirerapids
          binary_name: stwo_run_and_prove_recursive_tree

      - name: Build and upload stwo_run_and_prove_recursive_tree for graniterapids
        uses: ./.github/actions/build-arch-binary
        with:
          arch: graniterapids
          binary_name: stwo_run_and_prove_recursive_tree
