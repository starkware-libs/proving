name: CI

on:
  push:
    branches:
      - main
      - main-v[0-9].**
    tags:
      - v[0-9].**

  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - auto_merge_enabled

  merge_group:
    types:
      - checks_requested

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@master
        with:
          components: rustfmt
          toolchain: nightly-2025-04-06
      - uses: Swatinem/rust-cache@v2
      - run: scripts/rust_fmt.sh --check

  clippy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: dtolnay/rust-toolchain@master
        with:
          components: clippy
          toolchain: nightly-2025-04-06
      - uses: Swatinem/rust-cache@v2
      - run: scripts/clippy.sh

  js-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.2.10
      - run: scripts/js_typecheck.sh

  test-generated-code-in-stwo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: software-mansion/setup-scarb@v1
        with:
          scarb-version: "2.12.0"
      - uses: dtolnay/rust-toolchain@master
        with:
          components: rustfmt
          toolchain: nightly-2025-04-06
      - uses: Swatinem/rust-cache@v2
      - run: scripts/test_generated_code.py

  run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@master
        with:
          components: rustfmt
          profile: minimal
          toolchain: nightly-2025-04-06
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --release

  machete-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Machete
        uses: bnjbvr/cargo-machete@main

  all-tests:
    if: always()
    runs-on: ubuntu-latest
    needs:
    - clippy
    - format
    - run-tests
    - machete-check
    - test-generated-code-in-stwo
    steps:
    - name: Decide whether all the needed jobs succeeded or failed
      uses: re-actors/alls-green@v1.2.2
      with:
        jobs: ${{ toJSON(needs) }}
